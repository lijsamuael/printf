#include "main.h"
/**
 * _print_x - print number in lowecase hexadecimal
 * @ap: the list of arguments
 * Return: the length of the character
 */
int _print_x(va_list ap)
{
	int n = va_arg(ap, int);
	return (n);
}
